<Page x:Class="TimePunch_WinUI_StackedUI_Demo.Views.LogonView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:controls="using:TimePunch.StackedUI.Controls"
      xmlns:extensions="using:TimePunch.StackedUI.Extensions"
      xmlns:views="using:TimePunch_WinUI_StackedUI_Demo.Views"
      mc:Ignorable="d" 
      DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=LogonViewModel}"
      d:DesignHeight="450" d:DesignWidth="800"
      extensions:StackedFrameExtension.FrameKey="LogonView"
      Loaded="LogonView_OnLoaded">

    <controls:PageControl Title="Logon">
        <views:LogonDialog x:Name="LogonDialog"></views:LogonDialog>
    </controls:PageControl>
</Page>
