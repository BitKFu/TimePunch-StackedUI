<Window x:Class="TimePunch.StackedUI.Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimePunch_WPF_StackedUI_Demo"
        xmlns:stackedUi="clr-namespace:TimePunch.StackedUI;assembly=TimePunch-WPF-StackedUI"
        DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding BreadCrumbNavigationTitle}"></TextBlock>
        
        <stackedUi:StackedFrame Name="StackedFrame" Grid.Row="1">
        </stackedUi:StackedFrame>
    </Grid>
</Window>
