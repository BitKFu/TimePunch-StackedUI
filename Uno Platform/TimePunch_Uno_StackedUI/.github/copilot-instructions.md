# Copilot Instructions

## Project Guidelines
- For the fresh Uno migration project, keep LogonDialog and LogonViewModel included; do not remove the logon flow from the build.
- For this repository, the build folders must explicitly be named `bin` and `obj`; avoid using shortened names like `b` or `o`.